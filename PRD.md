# PRD — تطبيق التقدير السعري السريع للتشطيبات (اسم مؤقت: "تقدير")

## 1. نظرة عامة (Overview)

**المشكلة:** مقاولو التشطيبات في الجزائر يقضون ساعات إلى أيام لإعداد عرض سعر يدويًا (معاينة، حساب كميات بالعين، اتصال بالموردين، كتابة العرض)، مع عدم وجود أي أداة رقمية تناسب السوق المحلي (لا بيانات أسعار جزائرية، لا واجهة عربية).

**الحل:** تطبيق ويب يتيح للمقاول رفع صورة/عدة صور لغرفة + وصف نصي قصير، فيستخرج نظام الذكاء الاصطناعي تلقائيًا بنود العمل والكميات، ويطابقها مع **قائمة أسعار المقاول الخاصة** (التي أدخلها بنفسه مسبقًا)، لينتج عرض سعر جاهز خلال دقائق بدل ساعات.

**المستخدم المستهدف:** مقاول تشطيبات فردي أو صاحب مؤسسة صغيرة في الجزائر (دهان، بلاط، كهرباء أساسية).

**نطاق الـMVP:** تشطيب عام لغرفة واحدة (دهان + بلاط + كهرباء أساسية). لا CRM، لا فواتير، لا حسابات متعددة/فرق، لا دعم مناطق متعددة الأسعار.

---

## 2. القرار الجوهري: مصدر الأسعار

**لا توجد قاعدة بيانات أسعار جزائرية موثوقة في السوق حاليًا.** لذلك:

- كل مقاول يُدخل **قائمة أسعاره الخاصة** مرة واحدة عند التسجيل (Onboarding)، ويعدّلها متى شاء.
- النموذج الذكي **لا يخترع أسعارًا إطلاقًا**. دوره محصور في: تحليل الصورة/النص → استخراج بنود العمل وكمياتها (كـ JSON) → مطابقتها مع قائمة أسعار المقاول الموجودة فعليًا في قاعدة البيانات.
- تُدرَج قائمة أسعار افتراضية (Seed Template) تلقائيًا عند إنشاء أي حساب جديد، بأرقام تقريبية أولية، ليعدّلها المقاول فورًا بأسعاره الحقيقية بدل البدء من صفحة فارغة.
- هذا يبني تدريجيًا أول قاعدة بيانات أسعار جزائرية حقيقية (عبر تراكم بيانات المستخدمين لاحقًا) — لكن هذا خارج نطاق الـMVP.

---

## 3. قصص المستخدم (User Stories)

1. كمقاول جديد، أريد إنشاء حساب وإدخال/تعديل قائمة أسعاري (دهان، بلاط، كهرباء) حتى تعكس التقديرات أسعاري الحقيقية.
2. كمقاول، أريد رفع صورة (أو عدة صور) لغرفة مع وصف نصي قصير، حتى يحلل النظام المشروع تلقائيًا.
3. كمقاول، أريد رؤية تقدير أولي (بنود + كميات + أسعار + إجمالي) خلال ثوانٍ.
4. كمقاول، أريد تعديل أي بند (الكمية، السعر، الحذف، الإضافة اليدوية) قبل التأكيد، لأن الذكاء الاصطناعي قد يخطئ.
5. كمقاول، أريد توليد عرض سعر منسّق (PDF أو نص) يحمل اسمي/معلوماتي، جاهز لإرساله للزبون عبر واتساب.

---

## 4. نموذج البيانات (Data Model)

### جدول `contractors`
| الحقل | النوع | ملاحظات |
|---|---|---|
| id | UUID/INT PK | |
| name | TEXT | اسم المقاول/المؤسسة |
| phone | TEXT | اختياري في الـMVP |
| created_at | DATETIME | |

### جدول `price_items` (قائمة أسعار المقاول)
| الحقل | النوع | ملاحظات |
|---|---|---|
| id | UUID/INT PK | |
| contractor_id | FK → contractors | |
| category | TEXT | دهان / بلاط / كهرباء / أخرى |
| item_name | TEXT | مثال: "دهان جدران داخلي" |
| unit | TEXT | م² / نقطة / وحدة / بالمقطوع |
| unit_price | DECIMAL | بالدينار الجزائري |
| is_active | BOOLEAN | يسمح بتعطيل بند بدل حذفه |

### جدول `estimates` (التقديرات)
| الحقل | النوع | ملاحظات |
|---|---|---|
| id | UUID/INT PK | |
| contractor_id | FK | |
| client_name | TEXT | اختياري |
| room_type | TEXT | مطبخ/حمام/غرفة نوم/صالون... |
| area_m2 | DECIMAL | مستخرجة أو مُدخلة يدويًا |
| raw_description | TEXT | نص المقاول الأصلي |
| photo_paths | JSON/TEXT[] | مسارات الصور |
| status | ENUM | draft / final |
| created_at | DATETIME | |

### جدول `estimate_items` (بنود التقدير)
| الحقل | النوع | ملاحظات |
|---|---|---|
| id | UUID/INT PK | |
| estimate_id | FK | |
| price_item_id | FK (nullable) | فارغ إن كان بندًا يدويًا حرًا |
| item_name | TEXT | |
| quantity | DECIMAL | |
| unit | TEXT | |
| unit_price | DECIMAL | منسوخ وقت الإنشاء (لا يتغير لاحقًا مع تعديل price_items) |
| line_total | DECIMAL | quantity × unit_price |
| source | ENUM | ai_extracted / manual |

### قائمة الأسعار الافتراضية (Seed Template) — تُدرج تلقائيًا لكل مقاول جديد
| category | item_name | unit | unit_price (افتراضي، دج) |
|---|---|---|---|
| دهان | دهان جدران داخلي | م² | 400 |
| بلاط | تركيب بلاط أرضية | م² | 800 |
| بلاط | تركيب بلاط حائط | م² | 900 |
| كهرباء | نقطة كهرباء (تمديد+تركيب) | نقطة | 2500 |
| عام | تفكيك/تنظيف أرضية قديمة | م² | 200 |

*(هذه أرقام تقريبية أولية فقط لتسريع التجربة — لا تُعرض للمستخدم كحقيقة مطلقة، والمقاول يُشجَّع على تعديلها فور التسجيل.)*

---

## 5. تدفق العمل التقني (Technical Flow)

```
1. [المقاول يرفع صورة/صور + نص وصفي]
        ↓
2. [استدعاء نموذج الرؤية+النص (انظر القسم 6)]
   المدخلات للنموذج:
     - الصورة/الصور
     - النص الوصفي
     - قائمة أسماء بنود أسعار هذا المقاول (كمرجع/خيارات مسموحة فقط)
   المخرج المطلوب: JSON صارم بالشكل:
     {
       "room_type": "...",
       "area_m2": number | null,
       "items": [
         { "item_name": "<من قائمة المقاول فقط>", "quantity": number, "unit": "..." }
       ],
       "notes": "..."
     }
        ↓
3. [محرك المطابقة]
   لكل عنصر في items:
     - مطابقة اسمية مباشرة مع price_items الخاصة بالمقاول
     - إن لم يوجد تطابق: يُعرض كبند "غير مطابق" يدويًا للمقاول لربطه أو إدخال سعر يدوي
     - حساب line_total = quantity × unit_price
        ↓
4. [شاشة المراجعة]
   المقاول يعدّل الكميات/الأسعار/يضيف بندًا يدويًا/يحذف بندًا
        ↓
5. [توليد عرض السعر]
   PDF أو نص منسّق بالفصحى + زر مشاركة واتساب (رابط wa.me مع نص جاهز)
```

**قاعدة تصميم حرجة للـPrompt:** يجب إجبار النموذج على استخدام فقط أسماء البنود الموجودة في قائمة أسعار المقاول (تُمرَّر كقائمة خيارات مغلقة ضمن الطلب)، وعدم اختراع بنود أو أسعار جديدة. أي بند لا يجد له تطابقًا واضحًا يُترك للمقاول ليقرر يدويًا — الدقة والثقة أهم من "السحر الكامل".

---

## 6. نموذج الذكاء الاصطناعي (مجاني)

**الخيار الأساسي الموصى به: Google Gemini API (الطبقة المجانية)**
- يدعم الإدخال متعدد الوسائط (صورة + نص) ضمن نفس الطلب مجانًا بالكامل، بدون بطاقة بنكية.
- الطراز المقترح: **Gemini 2.5 Flash** (أو Flash-Lite إن احتجنا حدًا أعلى من الطلبات اليومية) — يوازن جيدًا بين الجودة والحدود المجانية السخية (حوالي 1,500 طلب/يوم على Flash، وأكثر على Flash-Lite).
- يدعم إخراج JSON منظم (JSON mode) مباشرة، وهو ما نحتاجه بالضبط لهذا التدفق.
- يدعم العربية بشكل جيد لكل من الوصف النصي المُدخل والإخراج.

**بديل احتياطي:** نماذج رؤية مجانية عبر **OpenRouter** (يوفر عدة نماذج رؤية بطبقة مجانية بالكامل دون بطاقة بنكية) — مفيد كخطة بديلة إذا واجهنا قيود Gemini، ويسمح بتبديل النموذج بسهولة لاحقًا دون تغيير بنية الكود (استدعاء بصيغة متوافقة مع OpenAI API).

**توصية معمارية:** طبقة استدعاء AI منفصلة (interface/adapter) في الكود بحيث يسهل تبديل المزوّد (Gemini ↔ OpenRouter ↔ أي نموذج آخر لاحقًا) دون إعادة كتابة بقية التطبيق.

---

## 7. الاستضافة (مجانية أولاً، قابلة للترقية)

**الخيار الموصى به: Render (الطبقة المجانية الدائمة)**
- استضافة ويب مجانية بشكل دائم، بدون بطاقة بنكية، مع قاعدة بيانات PostgreSQL مجانية مرفقة.
- القيد الوحيد المهم: الخدمة "تنام" بعد 15 دقيقة من عدم النشاط، وأول طلب بعدها يستغرق حوالي دقيقة للاستيقاظ — مقبول تمامًا لمرحلة تجربة أولى مع عدد محدود من المستخدمين.
- عند النمو: الانتقال لخطة Render المدفوعة الرخيصة، أو Railway/VPS رخيص (Hetzner)، هو مجرد تغيير في الاستضافة دون إعادة هيكلة الكود إن استُخدمت قاعدة بيانات قياسية (PostgreSQL) من البداية بدل SQLite.

**ملاحظة تقنية:** يُفضَّل استخدام PostgreSQL (المتوفرة مجانًا مع Render) بدل SQLite منذ البداية، لأن SQLite تفقد بياناتها عند إعادة نشر الخدمة على منصات كـRender (نظام ملفات غير دائم على الطبقة المجانية).

---

## 8. المكدس التقني (Tech Stack)

| الطبقة | الاختيار | السبب |
|---|---|---|
| Frontend + Backend | Next.js (App Router) | واجهة + API في مشروع واحد، سريع البناء |
| قاعدة البيانات | PostgreSQL (مجانية عبر Render) + Prisma ORM | دائمة، مجانية، متوافقة مع نمو لاحق |
| نموذج الذكاء الاصطناعي | Google Gemini API (مجاني) عبر طبقة adapter | رؤية+نص مجانًا، JSON mode |
| توليد PDF | `@react-pdf/renderer` | خفيف، يعمل داخل Next.js |
| تخزين الصور | مجلد محلي أو تخزين مجاني (مثل Cloudinary free tier) | يُقرَّر حسب قيود Render على الملفات الدائمة |
| الاستضافة | Render (Free tier) | مجاني بالكامل، بدون بطاقة، ترقية سلسة لاحقًا |

---

## 9. معايير النجاح لهذه الدورة

1. **التفاعل:** هل المقاولون الذين جرّبوا الأداة استخدموها أكثر من مرة؟
2. **السرعة الفعلية:** هل التقدير يصل خلال دقائق مقابل الساعات المعتادة يدويًا؟
3. **دقة الاستخراج:** هل نسبة البنود التي يحتاج المقاول لتصحيحها يدويًا منخفضة (أقل من 20%)؟

---

## 10. خارج النطاق صراحة (Out of Scope)

- CRM أو إدارة عملاء متقدمة
- فواتير وتحصيل دفعات
- حسابات/فرق متعددة المستخدمين
- دعم تسعير متعدد المناطق/الولايات
- تسجيل دخول متقدم (OAuth) — حساب بسيط بمعرّف/كلمة مرور أو حتى بدون تسجيل دخول في أول نسخة
- تحليل مخططات هندسية (تبقى للفئة الأخرى من الحلول المكتبية)
